import { describe, it, expect } from 'vitest'
import { parseParamString } from '../../src/parser/param'


describe.todo('COM-PARAM', () => {
    // const valid_param_string = 'lfo300:5'
    // const res = parseParamString(valid_param_string)
    // it('', () => {
    //     expect(res).toHaveProperty('name')
    //     expect(res).toHaveProperty('value')
    // })
    // it('', () => {
    //     expect(res.name).toBeTypeOf('string')
    //     expect(res.value).toBeTypeOf('number')
    // })
    // it('', () => {
    //     expect(res.name).toBe('lfo')
    //     expect(res.value).toBe('lfo')
    // })
})

